// loadedCargoList.js - 적재화물목록 / 혼재화물 적하목록 조회 전용
(function () {
  function bindEvents() {
    // 현재 페이지 전용 JS 없음
    // 사이드바 관련 코드는 절대 넣지 않음
  }

  document.addEventListener('DOMContentLoaded', bindEvents);
})();